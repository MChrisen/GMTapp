# Dependencies
node_modules/

# Build output — dist/ er committet så andre computere ikke skal bygge
# Kør `npm run build` lokalt efter kodeændringer og commit dist/ igen.

# Vite
.vite/

# Logs & local caches
*.log
.npm-cache/
.gmt-npm-cache/
.gmt-preview.log
.gmt-preview.pid
.gmt-desktop.log
.gmt-desktop.pid
.gmt-launch.log

# Environment (secrets — never commit real keys)
.env
.env.*.local

# OS
.DS_Store

# Editor (optional — uncomment if you prefer not to share IDE settings)
# .idea/
# .vscode/
